version: '3.8'

services:
  db:
    build: ./db/.
    container_name: db01
    restart: always
    env_file:
      - ./db/.env
    ports:
      - "5432:5432"
    volumes:
      - data:/var/lib/postgresql/data
    networks:
      - network

networks:
  network:

volumes:
  data: