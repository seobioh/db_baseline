version: '3.8'

services:
  db:
    build: ./db/.
    container_name: db01
    restart: always
    env_file:
      - ./db/.env
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    networks:
      - db_network

networks:
  db_network:

volumes:
  db_data:

